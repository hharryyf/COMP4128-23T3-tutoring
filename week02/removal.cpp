#include <bits/stdc++.h>
#define MAX_SIZE 511
using namespace std;

char s[MAX_SIZE];

int main() {
    int T;
    scanf("%d", &T);
    while (T-- > 0) {
        scanf("%s", s + 1);
        
    }
    return 0;
}