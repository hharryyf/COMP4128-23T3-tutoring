#include <bits/stdc++.h>
#define MAX_SIZE 200011
using namespace std;
typedef long long ll;

int N, M, A, B, C;

vector<int> g[MAX_SIZE];
ll p[MAX_SIZE];


int main() {
    int T, i;
    scanf("%d", &T);
    while (T-- > 0) {
        scanf("%d%d%d%d%d", &N, &M, &A, &B, &C);
        
    }
    return 0;
}