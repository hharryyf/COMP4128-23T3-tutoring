#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main() {
    int T;
    scanf("%d", &T);
    while (T-- > 0) {
        ll A, B;
        scanf("%lld%lld", &A, &B);
        
    }
    return 0;
}